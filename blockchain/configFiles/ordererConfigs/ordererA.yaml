# RegistryChain model for healthcare registries and repositories interchange with Hyperledger Fabric V2
# Author: Emeka Chukwu
#
# ---------------------------------------------------------------------------
# This is a configuration file for orderer for orgA
# ---------------------------------------------------------------------------
OrdererOrgs:
  # ---------------------------------------------------------------------------
  # Orderer
  # ---------------------------------------------------------------------------
  - Name: Orderer0
    Domain: orgA.com
    EnableNodeOUs: true
    # ---------------------------------------------------------------------------
    # SANS: (Optional) Specifies one or more Subject Alternative Name
    # to be set in the resulting x509.
    # ---------------------------------------------------------------------------
    Specs:
      - Hostname: Orderer0
        SANS:
          - localhost
